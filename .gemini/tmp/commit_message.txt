feat: Implement AI Mandala Chart generation feature

This commit implements the AI-powered Mandala Chart generation feature as per Issue #5:

- **src/lib/ai_client.ts:**
  - Added `generateMandalaChart` method to prompt AI for a structured Mandala Chart.
  - Implemented `callOllamaGenerateMandala` and `callGeminiGenerateMandala` for provider-specific API calls expecting JSON output.
  - Included logic for parsing AI responses and fallback mechanisms for invalid outputs or errors.

- **src/lib/firestore_service.ts:**
  - Added `updateMandalaChart` method to persist newly generated Mandala Charts to Firestore, updating the user's `AppData`.

- **src/app/page.tsx:**
  - Integrated UI for triggering Mandala Chart generation within the AI Chat tab.
  - Added state management (`generatedMandala`, `isGeneratingMandala`, `isMandalaPreviewOpen`, `userMandalaGoal`) for the generation process.
  - Implemented a preview `Dialog` to display AI-generated Mandala Chart structure to the user.
  - Developed `handleGenerateMandala` and `handleApplyGeneratedMandala` functions to orchestrate the generation and application of new Mandala Charts.

This completes the development of the AI-powered Mandala Chart generation functionality.