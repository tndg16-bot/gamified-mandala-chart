feat: Implement lesson import feature

This commit implements the lesson import feature as per Issue #4:

- **src/lib/lesson_parser.ts:**
  - Improved the parsing logic for lesson description and metadata extraction to be more robust.

- **src/components/LessonImportDialog.tsx:**
  - Updated the dialog description to reference the `docs/lesson_import_format.md` for detailed format.
  - Enhanced the `handleParse` function to include validation for required fields (Title, Description, Content) after parsing, providing better user feedback for invalid lesson formats.

- **src/lib/firestore_service.ts:**
  - Verified the existing `importLessons` method works as expected for saving parsed lessons to Firestore.

- **src/components/LessonList.tsx:**
  - Verified the component correctly displays imported lessons.

This completes the development of the lesson import functionality.